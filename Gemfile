source "https://rubygems.org"
gem "jekyll", "~> 4.2" # Or a recent version supported by GitHub Pages
# Add minima theme explicitly if needed, or other plugins
gem "minima", "~> 2.5"
gem "jekyll-feed", "~> 0.12"

# GitHub Pages specific dependencies (optional but good practice)
group :jekyll_plugins do
  gem "jekyll-paginate", "~> 1.1"
  gem "jekyll-sitemap", "~> 1.4"
  gem "jekyll-archives"
  # Add other plugins if you use them
end
